import React from 'react';
import styled from 'reshadow/macro';

import { Button } from './Button';

export const ListItem = ({ text, handler }) => styled(null)``(
  <li>
    <Button text={text} handler={handler} />
  </li>,
);
